defaults:
  - default

model:
  name: "math-llm"
  architecture: "gpt2"
  domain: "math"
  size: "medium"
  vocab_size: 50257
  hidden_size: 1024
  num_layers: 24
  num_heads: 16

data:
  preprocessing:
    min_text_length: 50
    max_text_length: 10000

training:
  max_epochs: 5
  learning_rate: 2e-5

hardware:
  use_gpu: "true"
  fp16: "true"