# Bangkong LLM Training System — Core Dependencies
# Install with: pip install -r requirements.txt
#
# For optional features, see pyproject.toml [project.optional-dependencies]
# Example: pip install -e ".[data,deploy,monitoring]"

# Core — required for training
torch>=1.13.0
transformers>=4.25.0
tokenizers>=0.13.0
accelerate>=0.15.0
numpy>=1.21.0
pyyaml>=6.0
pydantic>=1.10.0
psutil>=5.9.0
requests>=2.28.0
safetensors>=0.3.0
tqdm>=4.64.0
